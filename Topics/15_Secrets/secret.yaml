# ─────────────────────────────
# 📄 Secret Definition
# ─────────────────────────────
# This Secret stores sensitive application credentials.
# Values must be Base64 encoded before applying.

apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
data:
  # Base64 encoded username (example: echo -n admin | base64)
  DB_USERNAME: YWRtaW4=

  # Base64 encoded password (example: echo -n password123 | base64)
  DB_PASSWORD: cGFzc3dvcmQxMjM=
